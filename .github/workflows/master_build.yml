name: "🚀 TechRZN Master Build"

on:
  push:
    paths:
      - 'sources/**'
      - 'scripts/**'
      - 'manual_sources/**'  # <--- DIESE ZEILE MUSS REIN!
      - '.github/workflows/**'
  
  # --- NEU: TÄGLICHES AUTOMATISCHES UPDATE ---
  schedule:
    # Läuft jeden Tag um 04:00 Uhr UTC (ca. 05:00 Uhr in Kleve)
    - cron: '0 4 * * *'
  
  # Erlaubt das manuelle Starten per Button (sehr wichtig zum Testen!)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 🛰️ Repository auschecken
        uses: actions/checkout@v4

      - name: 🐍 Python einrichten
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 Abhängigkeiten installieren
        run: pip install requests

      - name: 🛠️ Master-Liste bauen
        run: python scripts/build.py

      - name: 💾 Änderungen speichern (Commit & Push)
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "build: Filter-Listen und Whitelist aktualisiert [bot]"
          # Hier speichern wir ALLES ab, was im Build-Lauf entstanden ist
          file_pattern: 'blocklists/*.txt Whitelists/*.txt'
          push_options: '--force'
